You can find the backend in the folder named Backend. Refer to this for APIs. How to send data from frontend (payload) and what is received on API calls and what to show in UI. Make changes in backend if required or asked to do
Use Skills vercel-react-best-practices. Using these is mandatory according to use case
When creating new pages, reference other pages for structure/ui
I do not want framer motion animations anywhere in whole project except the sidebar
Use bun install if any new package is to be installed. Not npm install
Always use Shadcn components. Look for components of shadcn on web, install if not installed. Use shadcn skills to find components or details