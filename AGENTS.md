You can find the backend in the folder named taj-hrms-backend. Refer to this for APIs. How to send data from frontend (payload) and what is received on API calls and what to show in UI. Make changes in backend if required or asked to do.
Use Skills from .agent OR .agents folder. framer-motion-animator for motion, tanstack-query for data fetching, vercel-react-best-practices. Using these is mandatory according to use case. First understand the project and task then read the skills and apply accordingly. This flow is mandatory.
When creating new pages, reference other pages for structure/ui
I do not want framer motion animations anywhere in whole project except the sidebar
Use bun install if any new package is to be installed. Not npm install